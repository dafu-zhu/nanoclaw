\documentclass[11pt,a4paper]{article}

% === PACKAGES ===
\usepackage[utf8]{inputenc}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{enumitem}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{fancyhdr}

% === DOCUMENT INFO (CUSTOMIZE THESE) ===
\newcommand{\studentname}{[STUDENT NAME]}
\newcommand{\coursename}{[COURSE NAME]}
\newcommand{\coursenumber}{[COURSE NUMBER]}
\newcommand{\assignmentname}{Problem Set [X]}
\newcommand{\duedate}{[DUE DATE]}

% === HEADER/FOOTER ===
\pagestyle{fancy}
\fancyhf{}
\lhead{\studentname}
\chead{\coursename}
\rhead{\assignmentname}
\rfoot{Page \thepage}

% === SOLUTION ENVIRONMENT ===
\newenvironment{solution}{%
    \par\medskip\noindent\textbf{Solution.}\enspace
}{%
    \par\medskip
}

% === COMMON MATH OPERATORS (add more as needed) ===
\DeclareMathOperator{\E}{\mathbb{E}}
\DeclareMathOperator{\Var}{Var}
\DeclareMathOperator{\Cov}{Cov}
\DeclareMathOperator{\tr}{tr}
\DeclareMathOperator{\rank}{rank}
\newcommand{\R}{\mathbb{R}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\Z}{\mathbb{Z}}

% === TITLE ===
\title{\textbf{\coursename} (\coursenumber) \\ \assignmentname}
\author{\studentname}
\date{Due: \duedate}

\begin{document}

\maketitle

% === FORMATTING GUIDELINES (delete before submission) ===
% 1. Use \begin{equation} for important formulas, not \[ \]
% 2. Use \begin{align*} for multi-step derivations
% 3. NO \boxed{} around answers unless course style requires it
% 4. State conclusions naturally in prose
% 5. Use \newpage before each new problem
% 6. For multi-part problems: use (a), (b), (c) inline - NO subsections

% === PROBLEM 1 ===
\section*{Problem 1}
[Copy the full problem statement here exactly as given.]

\begin{solution}
[Your solution here.]

For multi-part problems:

(a) [Solution for part (a).]

(b) [Solution for part (b).]

Use equations when needed:
\begin{equation}
    f(x) = ax^2 + bx + c
\end{equation}

For derivations:
\begin{align*}
    \text{LHS} &= \text{step 1 explanation}\\
    &= \text{step 2 explanation}\\
    &= \text{RHS (final result)}
\end{align*}

Conclude with a clear statement of the answer.
\end{solution}

\newpage

% === PROBLEM 2 ===
\section*{Problem 2}
[Problem statement.]

\begin{solution}
[Solution.]
\end{solution}

% Add more problems as needed...

\end{document}
